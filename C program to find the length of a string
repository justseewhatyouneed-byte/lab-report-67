#include <stdio.h>
#include <string.h>

int main() {
    char str[100];

    printf("Enter a string: ");
    scanf("%s", str); // For single word input. Use fgets for full sentence

    int length = strlen(str);

    printf("Length of the string is: %d\n", length);

    return 0;
}
