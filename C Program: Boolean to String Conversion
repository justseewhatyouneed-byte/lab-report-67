#include <stdio.h>
#include <stdbool.h>

const char* boolToString(bool value) {
    return value ? "true" : "false";
}

int main() {
    bool a = true;
    bool b = false;

    printf("a = %s\n", boolToString(a));
    printf("b = %s\n", boolToString(b));

    return 0;
}
